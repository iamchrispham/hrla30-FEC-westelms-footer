import React from 'react';

const Carousel = (props) => { // reminder: use ES6 syntax for stateless components
  return (
    <div className="carousel-cp" id="carousel-cp">
      This is a carousel div.
    </div>
  )
}

export default Carousel;